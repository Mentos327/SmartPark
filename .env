# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=9a8ba81691dc705c7ec90ee0bb37e27ca3dd0dfced97863a05c7094a9f447420

# Where the SQLite database file lives. Created automatically if missing.
DATABASE_PATH=instance/smart_parking.db

# HTTP server
HOST=127.0.0.1
PORT=5000
DEBUG=true

# Outgoing email (Gmail example — use an App Password, not your normal password)
MAIL_USERNAME=www.magwekim@gmail.com
MAIL_PASSWORD=nmzk lkcv bsii faun

# M-Pesa (global settings; per-location till/passkey/keys are set by each
# admin on the Admin -> Pricing & Payments page in the app itself)
MPESA_CALLBACK_URL=https://your-public-domain.com/api/payments/mpesa/callback
MPESA_ENV=sandbox
MPESA_CALLBACK_SECRET=another-long-random-string
MPESA_SIMULATION=true
# true = mpesa checkout must be confirmed by Safaricom's callback before
# it's marked paid. false = guard confirmation alone is enough (like cash).
MPESA_REQUIRE_PAYMENT_CONFIRMATION=false